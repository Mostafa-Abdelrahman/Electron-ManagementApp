CREATE TABLE employees (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR,
    phone INT,
    salary FLOAT,
    job VARCHAR,
    residence VARCHAR,
    employed_data DATE,
    notes VARCHAR
);
